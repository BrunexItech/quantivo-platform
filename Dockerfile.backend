FROM node:20-alpine

WORKDIR /app

COPY backend/package*.json ./

# Install ALL dependencies (including dev dependencies like nodemon)
RUN npm ci

COPY backend/ .

RUN mkdir -p uploads

EXPOSE 5000

CMD ["node", "server.js"]